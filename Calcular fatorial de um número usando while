#include <stdio.h>

int main() {
    int num, fatorial = 1, i = 1;

    printf("Digite um número: ");
    scanf("%d", &num);

    while (i <= num) {
        fatorial *= i;
        i++;
    }

    printf("O fatorial de %d é: %d\n", num, fatorial);
    return 0;
}
