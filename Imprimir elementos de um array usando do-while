#include <stdio.h>

int main() {
    int arr[] = {1, 2, 3, 4, 5};
    int i = 0;
    int n = sizeof(arr) / sizeof(arr[0]);

    do {
        printf("%d\n", arr[i]);
        i++;
    } while (i < n);

    return 0;
}
